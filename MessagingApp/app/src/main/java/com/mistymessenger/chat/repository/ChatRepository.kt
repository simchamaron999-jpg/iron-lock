package com.mistymessenger.chat.repository

import com.mistymessenger.core.network.RetrofitClient
import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class ChatRepository @Inject constructor(retrofitClient: RetrofitClient) {
    // API service bindings wired in later phases
}
